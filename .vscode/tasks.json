{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "command": "cmake --build build/ --parallel 4 ; ${workspaceFolder}/build/Debug/Ligid.exe",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },

        {
            "label": "configure",
            "type": "shell",
            "command": "cmake -S . -B build/",
            "problemMatcher": []
        }
    ]
}